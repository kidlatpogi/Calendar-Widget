const fs = require('fs');
const path = './lib/event-parser.js';
const disk = fs.readFileSync(path, 'utf8');
console.log('disk contains signature line:', disk.includes('static expandRecurringEvent'));
console.log('disk snippet:\n', disk.split('\n').slice(230,320).join('\n'));
const parser = require('../lib/event-parser');
console.log('\nrequire.resolve ->', require.resolve('../lib/event-parser'));
console.log('exported fn params ->', parser.expandRecurringEvent.length);
console.log('exported fn toString snippet:\n', parser.expandRecurringEvent.toString().split('\n').slice(0,30).join('\n'));
console.log('\nList top-level module.exports keys:', Object.keys(module.exports || {}));
